<div>
    {{!-- Vehicle Data --}}
    <section class="data-section">
        <div class="data-table">
            <div class="d-flex justify-content-between">
                <h3 style="display: inline;">Vehicle</h3>
                <div>
                    <button class="btn btn-edit" id="toggle-modal-vehicle"><i class="bi bi-pencil"></i></button>
                    <button class="btn btn-delete" id="delete-vehicle-btn">remove</button>
                </div>
            </div>
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>Vin</th>
                        <th>Type</th>
                        <th>Make</th>
                        <th>Model</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td id="vin">{{vehicle.vin}}</td>
                        <td>{{vehicle.type}}</td>
                        <td>{{vehicle.make}}</td>
                        <td>{{vehicle.model}}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div id="edit-vehicle-form" class="hidden">
            <form class="mt-3">
                <label for="type" class="form-label">Type:</label>
                <input name="type" type="text" value="{{vehicle.type}}" class="form-control">
                <label for="make" class="form-label">Make:</label>
                <input name="make" type="text" value="{{vehicle.make}}" class="form-control">
                <label for="model" class="form-label">Model:</label>
                <input name="model" type="text" value="{{vehicle.model}}" class="form-control">
                {{!-- <label for="purchased-date" class="form-label">Purchased Date:</label>
                <input name="purchased-date" type="date" value="{{vehicle.purchased}}" class="form-control"> --}}
                <button type="submit" class="btn btn-primary" id="update-vehicle-form">Update</button>
            </form>
        </div>
    </section>
    {{!-- End Vehicle Data --}}

    {{!-- Registration Data --}}
    <section class="data-section">
        <div class="data-table">
            {{#if vehicle.registration}}
            <div class="d-flex justify-content-between">
                <h3>Registration</h3>
                <div>
                    <button class="btn btn-edit" id="toggle-modal-registration"><i class="bi bi-pencil"></i></button>
                    <button class="btn btn-delete" id="delete-registration-btn">remove</button>
                </div>
            </div>
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>State</th>
                        <th>Issued</th>
                        <th>Expires</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>{{vehicle.registration.state}}</td>
                        <td>{{vehicle.registration.issued_date}}</td>
                        <td>{{vehicle.registration.expiration_date}}</td>
                    </tr>
                </tbody>
            </table>
            {{else}}
            <button class="btn btn-primary" id="toggle-modal-registration">add registration</button>
            {{/if}}
        </div>

        <div id="edit-registration-form" class="hidden">
            <form class="mt-3">
                <label for="state" class="form-label">State:</label>
                <input name="state" type="text" value="{{vehicle.registration.state}}" class="form-control">
                <label for="issued" class="form-label">Issued Date:</label>
                <input name="issued" type="date" class="form-control">
                <label for="expires" class="form-label">Expiration Date:</label>
                <input name="expires" type="date" class="form-control">
                {{#if vehicle.registration}}
                <button type="submit" class="btn btn-primary" id="update-registration-form">Update</button>
                {{else}}
                <button type="submit" class="btn btn-primary" id="add-registration-btn">Add</button>
                {{/if}}
            </form>
        </div>
    </section>
    {{!-- End Registration Data --}}

    {{!-- Insurance Data --}}
    <section class="data-section">
        <div class="data-table">
            {{#if vehicle.insurance}}
            <div class="d-flex justify-content-between">
                <h3>Insurance</h3>
                <div>
                    <button class="btn btn-edit" id="toggle-modal-insurance"><i class="bi bi-pencil"></i></button>
                    <button class="btn btn-delete" id="delete-insurance-btn">remove</button>
                </div>
            </div>
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>Company</th>
                        <th>Policy</th>
                        <th>Starts</th>
                        <th>Ends</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>{{vehicle.insurance.company}}</td>
                        <td>{{vehicle.insurance.policy_number}}</td>
                        <td>{{vehicle.insurance.start_date}}</td>
                        <td>{{vehicle.insurance.end_date}}</td>
                    </tr>
                </tbody>
            </table>
            {{else}}
            <button class="btn btn-primary" id="toggle-modal-insurance">add insurance</button>
            {{/if}}
        </div>

        <div id="edit-insurance-form" class="hidden">
            <form class="mt-3">
                <label for="company" class="form-label">Company:</label>
                <input name="company" type="text" value="{{vehicle.insurance.company}}" class="form-control">
                <label for="policy-number" class="form-label">Policy Number:</label>
                <input name="policy-number" type="text" value="{{vehicle.insurance.policy_number}}"
                    class="form-control">
                <label for="start-date" class="form-label">Start Date:</label>
                <input name="start-date" type="date" class="form-control">
                <label for="end-date" class="form-label">End Date:</label>
                <input name="end-date" type="date" class="form-control">
                {{#if vehicle.insurance}}
                <button type="submit" class="btn btn-primary" id="update-insurance-form">Update</button>
                {{else}}
                <button type="submit" class="btn btn-primary" id="add-insurance-btn">Add</button>
                {{/if}}
            </form>
        </div>
    </section>
    {{!-- End Insurance Data --}}
</div>

<script src="../javascript/updateInfo.js"></script>
<script src="../javascript/insurance.js"></script>
<script src="../javascript/registration.js"></script>